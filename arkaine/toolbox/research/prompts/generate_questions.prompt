The original research topic is:

```{topic}```

{questions}

Your findings so far are:

{findings}

Given these findings and your work so far, generate a list of followup questions to query via SERP to guide further research. Answer only in a numbered form to make it easy to parse.